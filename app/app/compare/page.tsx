import { PhaseStub } from '@/components/layout/PhaseStub';

export default function ComparePage() {
  return (
    <PhaseStub
      phase={8}
      feature="Compare two captures side by side."
      description="Dual-viewer layout with independent file selection per panel, comparison drafts, and consolidated PDF reports."
    />
  );
}
