import { PhaseStub } from '@/components/layout/PhaseStub';

export default function RoomExplorerPage() {
  return (
    <PhaseStub
      phase={5}
      feature="Room-first file explorer."
      description="Files grouped by date for a single room, with the same thumbnail grid and click-to-view behavior as the project explorer."
    />
  );
}
